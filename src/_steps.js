/**
 * Steps to use firebase
 * 1. create a project on console.firebase.google.com 
 * install firebase
 * register web app in firebase
 * copy firbase init with config fro firebase project setting into file firebase.init.js 
 * export dafult app
 * import app firebase.init.js into app.js
 *  turn on google authentication (firebase>authentication>sign in google)
 * import getAuth from firebase/auth and create auth=getAuth(app)
 * create google provider
 * 
 */